import {UserDetails} from "./UserDetails";

export const ShowUser = () => {
  const arr = [1,2,3,4,5];
  arr.map((e) => console.log(e));

  return (
      <div>
          {/*<UserDetails/>*/}
      </div>
  )
}