import {useState} from "react";

export const ValueColorChange = () => {
    const [value, setValue] = useState();

    return (
        <div>
            <h1 className="fs-1 fw-bolder font-monospace text-center mt-5"></h1>
        </div>
    )
}